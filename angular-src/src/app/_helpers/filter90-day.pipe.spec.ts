import { Filter90DayPipe } from './filter90-day.pipe';

describe('Filter90DayPipe', () => {
  it('create an instance', () => {
    const pipe = new Filter90DayPipe();
    expect(pipe).toBeTruthy();
  });
});
