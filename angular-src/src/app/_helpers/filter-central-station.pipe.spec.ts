import { FilterCentralStationPipe } from './filter-central-station.pipe';

describe('FilterCentralStationPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCentralStationPipe();
    expect(pipe).toBeTruthy();
  });
});
