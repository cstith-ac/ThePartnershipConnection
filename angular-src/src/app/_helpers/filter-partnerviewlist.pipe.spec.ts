import { FilterPartnerviewlistPipe } from './filter-partnerviewlist.pipe';

describe('FilterPartnerviewlistPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPartnerviewlistPipe();
    expect(pipe).toBeTruthy();
  });
});
